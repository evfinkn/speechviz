{
  "name": "speechviz",
  "version": "0.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "postinstall": "patch-package && mkdir public && mkdir public/{css,js,audio,waveforms,segments}",
    "build": "webpack",
    "postbuild": "cp src/style.css public/css/ && cp src/index.html public/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "npm run build",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.73.0",
    "webpack-cli": "^4.10.0"
  },
  "dependencies": {
    "express": "^4.18.1",
    "mime": "^3.0.0",
    "patch-package": "^6.4.7",
    "peaks.js": "^2.0.2"
  }
}
